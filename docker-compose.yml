# version: '1.0'
services:
  postgres:
    image: postgres:13
    container_name: postgres
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: 1234
      POSTGRES_DB: project_capstone
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - network_microservice

  backend:
    container_name: backend
    build:
      context: .
      dockerfile: Backend/Dockerfile
    image: jarntae/backend:1.0.0
    ports:
      - "8000:8000"
    depends_on:
      - postgres
    restart: unless-stopped
    env_file:
      - backend/.env
    networks:
      - network_microservice
      - network_Webapp


  frontend:
    container_name: frontend
    build:
      context: .
      dockerfile: Frontend/Dockerfile
    image: jarntae/frontend:1.0.0
    depends_on:
      - backend
    restart: always
    ports:
      - "5173:5173"
    networks:
      - network_Webapp
      - network_microservice





  nginx:
    container_name: nginx
    build:
      context: .
      dockerfile: nginx/Dockerfile
    image: jarntae/nginx:1.0.0
    ports:
      - 80:80
    depends_on:
      - frontend
      - backend
    networks:
      - network_Webapp
      - network_microservice
    restart: always


networks:
  network_Webapp:
  network_microservice:

volumes:
  postgres_data:
